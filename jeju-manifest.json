{
  "$schema": "../../jeju-manifest.schema.json",
  "name": "otc-desk",
  "displayName": "OTC Trading Desk",
  "version": "1.0.0",
  "type": "vendor",
  "description": "Over-the-counter trading desk for large token swaps",
  "commands": {
    "dev": "bun run dev",
    "build": "bun run build",
    "start": "bun run start"
  },
  "ports": {
    "main": 5004,
    "api": 5005
  },
  "dependencies": ["contracts", "config"],
  "optional": true,
  "enabled": true,
  "autoStart": true,
  "tags": ["defi", "trading", "otc"]
}

